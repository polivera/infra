# .sops.yaml
creation_rules:
  - path_regex: \.enc\.yaml$
    gpg: 'YOUR_GPG_KEY_ID'  # Replace with your GPG key ID
  - path_regex: secrets\.enc\.yaml$
    gpg: 'YOUR_GPG_KEY_ID'  # Replace with your GPG key ID